<% if(page === 'Home') { %>
    <% include pages/home.ejs %>
<% } else if(page === 'Login') { %>
    <% include pages/login.ejs %>
<% } else if(page === 'Primeiro acesso') { %>
    <% include pages/primeiro-acesso.ejs %>
<% } else if(page === 'Esqueci minha senha') { %>
    <% include pages/esqueci-minha-senha.ejs %>
<% } else if(page === 'Recuperar senha') { %>
    <% include pages/alterar-senha.ejs %>
<% } else if(page === 'Termos de uso') { %>
    <% include pages/termos-de-uso.ejs %>
<% } else if(page === 'Política de privacidade') { %>
    <% include pages/politica-de-privacidade.ejs %>
<% } else if(page === 'Finalizar compra') { %>
    <% include pages/finalizar-compra.ejs %>
<% } else if(page === 'Detalhes do pedido') { %>
    <% include pages/detalhes-do-pedido.ejs %>
<% } else if(page === 'Minha conta') { %>
    <% include pages/minha-conta.ejs %>
<% } else if(page === 'Alterar senha') { %>
    <% include pages/minha-conta-alterar-senha.ejs %>
<% } else if(page === 'Pedidos') { %>
    <% include pages/minha-conta-pedidos.ejs %>
<% } else if(page === 'Segmentação') { %>
    <% include pages/segmentacao.ejs %>
<% } %>